file_config:
  name: File Configuration
  comment:
    This key defines file-specific configuration without configuring a collection
    or modifying files.
  description: >-
    This key defines file-specific configuration without configuring a collection
    or modifying files.
  examples:
    - |-
      file_config:
        - glob: data/*.yml
          _inputs:
            title:
              comment: This is a YAML data file title.
  documentation_link: https://cloudcannon.com/documentation/articles/configuration-file-reference/#file_config
type.FileConfigEntry:
  name: File Configuration Entry
  comment:
  description: Provides scope to configure at a file level, without adding configuration to files.
  examples:
  documentation_link:
