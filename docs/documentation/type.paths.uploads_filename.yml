gid: type.paths.uploads_filename
url: /configuration-file/types/paths/uploads_filename/
title: ''
description: This key defines the path for the name of the uploaded file.
examples:
  - description: >-
      In this example, we have configured the uploads filename path for the
      `blog` Collection.
    language: yaml
    code: |-
      collections_config:
        blog:
          paths:
            uploads_filename: '{filename|slugify}'
    annotations: []
    source: /cloudcannon.config.yml
show_in_navigation: false
