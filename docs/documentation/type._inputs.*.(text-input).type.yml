gid: type._inputs.*.(text-input).type
url: /types/_inputs/*/(text-input)/type/
title: Type
description: >-
  This key defines the input type, which controls how this input appears and
  behaves.
examples:
  - description: >-
      In this example, we have configured the `title` key as a *Text Input*
      type.
    language: yaml
    code: |-
      _inputs:
        title:
          type: text
    annotations: []
    source: cloudcannon.config.yml
show_in_navigation: false
