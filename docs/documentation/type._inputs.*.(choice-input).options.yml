gid: type._inputs.*.(choice-input).options
url: /types/_inputs/*/(choice-input)/options/
title: ''
description: This key defines options that are specific to Choice Inputs.
examples:
  - description: >-
      In this example, we have configured *Choice Input* options including
      values.
    language: yaml
    code: |-
      _inputs:
        status:
          type: choice
          options:
            values:
              - draft
              - published
              - archived
    annotations: []
    source: cloudcannon.config.yml
show_in_navigation: false
