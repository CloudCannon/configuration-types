gid: type._inputs.*.(multichoice-input).options
url: /types/_inputs/*/(multichoice-input)/options/
title: ''
description: This key defines options that are specific to Multichoice Inputs.
examples:
  - description: >-
      In this example, we have configured *Multichoice Input* options including
      values.
    language: yaml
    code: |-
      _inputs:
        categories:
          type: multichoice
          options:
            values:
              - technology
              - design
              - business
    annotations: []
    source: /cloudcannon.config.yml
show_in_navigation: false
