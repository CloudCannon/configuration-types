gid: type.snippet.params.*.(content-parser-config)
url: /configuration-file/types/snippet/params/*/(content-parser-config)/
title: Content Parser Configuration
description: >-
  This key defines a parser configuration that parses rich multiline content,
  such as the content between paired tags. Can be configured to parse nested
  snippets within.
examples:
  - description: >-
      In this example, we have configured a content parser to parse rich
      multiline content between paired tags.
    language: liquid
    code: |-
      {% highlight "js" %} let a = b; {% endhighlight %}
                          └──────────┘
    annotations: []
show_in_navigation: false
