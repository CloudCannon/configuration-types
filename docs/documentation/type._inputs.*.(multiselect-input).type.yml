gid: type._inputs.*.(multiselect-input).type
url: /configuration-file/types/_inputs/*/(multiselect-input)/type/
title: ''
description: >-
  This key defines the input type, which controls how this input appears and
  behaves.
examples:
  - description: >-
      In this example, we have configured the `tags` key as a *Multiselect
      Input* type.
    language: yaml
    code: |-
      _inputs:
        tags:
          type: multiselect
    annotations: []
    source: /cloudcannon.config.yml
show_in_navigation: false
