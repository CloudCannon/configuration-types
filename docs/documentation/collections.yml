collections_config:
  title:
  description: Definitions for your collections, which are the sets of content files for your site grouped by folder.
  examples:
collections_config.*:
  title:
  description: Definitions for your Collections, which are the sets of content files for your site grouped by folder.
  examples:
collections_config.*._inputs:
  title:
  description: Controls the behavior and appearance of your inputs in data editing interfaces inside a Collection.
  examples:
collections_config.*._editables:
  title:
  description: Configuration for editable regions in the Visual Editor, including content, block, link, image, and text editing options. These options are specific to a Collection.
  examples:

collection_groups:
  title:
  description: Defines which collections are shown in the site navigation and how those collections are grouped.
  examples:
collection_groups.[*]:
  title: Collection Group
  description: A group of Collections shown in the Site Navigation.
  examples:
