gid: type._editables.*.styles
url: /configuration-file/types/_editables/*/styles/
title: ''
description: >-
  This key defines the path to the CSS file containing the styles CloudCannon
  offers in a dropdown menu in your WYSIWYG toolbar.


  Each style is the combination of an element and a class name. The value can be
  the path to the source or the build-output CSS file.
examples:
  - description: >-
      In this example, we have enabled a dropdown menu in our WYSIWYG toolbar to
      apply a style to selected text. The styles are defined in the
      `/css/styles.css` file.
    language: yaml
    code: |-
      _editables:
        content:
          styles: /css/styles.css
    annotations: []
    source: /cloudcannon.config.yml
  - description: >-
      This example file defines the styles that CloudCannon can add to your
      WYSIWYG toolbar.
    language: css
    code: |-
      /* Can be applied to blocks of content */
      p.callout {
        margin: 10px;
        border: 1px solid #f5f5f5;
        background-color: #eee;
      }

      /* Can be applied to inline content */
      span.big-blue-text {
        font-size: 2rem;
        color: blue;
      }

      /* Applied to content, excluded from style dropdown */
      h2 {
        font-family: cursive;
      }

      /* Applied to content, excluded from style dropdown */
      .align-left { text-align: left; }
      .align-center { text-align: center; }
      .align-right { text-align: right; }
      .align-justify { text-align: justify; }
    annotations: []
    source: /css/styles.css
show_in_navigation: false
