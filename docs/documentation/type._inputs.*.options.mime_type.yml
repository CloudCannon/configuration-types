gid: type._inputs.*.options.mime_type
url: /configuration-file/types/_inputs/*/options/mime_type/
title: ''
description: This key defines what format an image is converted to prior to upload.
examples:
  - description: >-
      In this example, we have configured an *Image Input* to convert images to
      WebP format prior to upload.
    language: yaml
    code: |-
      _inputs:
        hero_image:
          type: image
          options:
            mime_type: image/webp
    annotations: []
    source: /cloudcannon.config.yml
show_in_navigation: false
