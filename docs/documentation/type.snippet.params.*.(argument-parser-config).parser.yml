gid: type.snippet.params.*.(argument-parser-config).parser
url: /types/snippet/params/*/(argument-parser-config)/parser/
title: ''
description: |-
  This key defines the parser type for argument parser configurations.

  The value must be `argument`. This specifies that the parameter uses the argument parser, which parses a single argument value.

examples:
  - description: In this example, we have configured an argument parser for a snippet parameter.
    language: yaml
    code: |-
      _snippets:
        example:
          snippet: "<<example [[param]]>>"
          params:
            param:
              parser: argument
              options:
                model:
                  editor_key: example_id
    annotations: []
    source: cloudcannon.config.yml
show_in_navigation: false
