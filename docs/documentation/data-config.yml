data_config:
  title: Data Configuration
  description: Controls what data sets are available to populate select and multiselect inputs.
  examples:
    - |-
      data_config:
        locations:
          path: data/locations.yml
        authors:
          path: data/authors
data_config.*:
  title: Data Configuration Entry
  description: Controls what data sets are available to populate select and multiselect inputs.
  examples:
data_config.*.path:
  title: Path
  description: The path to a file or folder of files containing data.
  examples:
