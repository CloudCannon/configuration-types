gid: collections_config.*.add_options.[*].(add-option).icon
url: /collections_config/*/add_options/[*]/(add-option)/icon/
title: Icon
description: >-
  The icon next to the text in the menu item. Defaults to using icon from the
  matching schema if set, then falls back to add.
examples: []
